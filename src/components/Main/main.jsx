import React from 'react';
import { Card, CardHeader, CardContent, Typography, Divider} from '@material-ui/core'

const Main = () => {

  return (
    <div>
      The main component
    </div>
  )
}

export default Main;