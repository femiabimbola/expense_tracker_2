import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
   income: {
     borderBottom: '12px solid rgba(0, 255, 0, 0.8)',
   },

   expense: {
    borderBottom: '12px solid rgba(255, 0, 0, 0.8)',
   }
}));